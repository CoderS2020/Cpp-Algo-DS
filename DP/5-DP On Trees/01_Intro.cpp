/*
int res=Initialize;
int solve(Node * root, int & res){
    if(root==NULL) return 0;

    int left=solve(root->left,res);
    int right=solve(root->right,res);

    int temp= calculate temp ans
    int ans=max(temp,some relation);
    
    res=max(res,ans);
    
    return temp;
}
*/